services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://your-domain.com}
    ports:
      - "127.0.0.1:${PORT:-3001}:80"
    restart: unless-stopped
